import { Navigation } from './navigation.model';
import { Settings } from './settings.model';

export interface Score40 {
    title: string,
    headline: string,
    subheading: string,
    navigation: Navigation,
    settings: Settings
}