import { Settings } from 'src/app/models/settings.model';

export interface Score40ResultsQuizBreakdown {
    carouselHeadline: string;
    headline: string;
    id: string;
    settings: Settings;
    subheading: string;
}
